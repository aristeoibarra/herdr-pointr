{
  "name": "herdr-pointr",
  "version": "2.1.1",
  "description": "Point at a DOM element on a localhost page and it lands in the coding agent that owns that project, with its React component, selector, styles and a screenshot.",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsup && CGO_ENABLED=0 go build -trimpath -ldflags=\"-s -w\" -o dist/pointr ./bridge",
    "typecheck": "tsc --noEmit && go vet ./bridge",
    "test": "go test ./bridge"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aristeoibarra/herdr-pointr.git"
  },
  "keywords": [
    "herdr",
    "herdr-plugin",
    "coding-agent",
    "devtools",
    "browser",
    "react"
  ],
  "author": "aristeoibarra <aristeoibarra608@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@medv/finder": "^4.0.2",
    "modern-screenshot": "^4.7.0",
    "tsup": "^8.5.1",
    "typescript": "^5.7.0"
  }
}
